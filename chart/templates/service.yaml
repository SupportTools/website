apiVersion: v1
kind: Service
metadata:
  name: website
  labels:
    app: website
    team: supporttools
    component: website
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: website
    team: mattox
    component: website